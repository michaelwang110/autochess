def algo(pieces_on_board, piece_dict):
    # 1 gold
    if len(pieces_on_board) == 0:
    # 1, 2 gold
    elif len(pieces_one_board) <= 1:
    # 1, 2, 3 gold
    elif len(pieces_on_board) <= 3:
    # 1, 2, 3, 4 gold
    elif len(pieces_on_board) <= 6:
    # 1, 2, 3, 4, 5 gold:
    else:
        
    return pieces_ranking